# hesap-makinesi

Basit bir Python hesap makinesi.

Çalıştırmak için repo kökünde bulunan `hesap.py` dosyasını kullanabilirsiniz.

Örnek kullanım:

```bash
python3 hesap.py
```

Kod (örnek):

```python
ilk sayıyı giriniz: 5
2. sayıyı giriniz: 3
hangi işlem olsun (toplama, çıkarma, çarpma, bölme): toplama
sonuç = 8
